import React from 'react';
import { Link } from 'react-router';

const Settings = () => (
    <div className="container">
        <h1>Here are the Settings! Oh... There are no settings...</h1>
        <Link to="/">Go Back</Link>
    </div>
);

export default Settings;

